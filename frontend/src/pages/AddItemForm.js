import React from "react";
import { Typography } from "@mui/material";

const AddItemForm = () => (
  <Typography variant="h4" sx={{ p: 3, color: "#4caf50" }}>
    Add New Item
  </Typography>
);

export default AddItemForm;
