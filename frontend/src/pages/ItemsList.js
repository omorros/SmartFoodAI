import React from "react";
import { Typography } from "@mui/material";

const ItemsList = () => (
  <Typography variant="h4" sx={{ p: 3, color: "#2196f3" }}>
    All Stored Items
  </Typography>
);

export default ItemsList;
