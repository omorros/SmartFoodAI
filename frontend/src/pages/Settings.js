import React from "react";
import { Typography } from "@mui/material";

const Settings = () => (
  <Typography variant="h4" sx={{ p: 3, color: "#f57c00" }}>
    App Settings
  </Typography>
);

export default Settings;
